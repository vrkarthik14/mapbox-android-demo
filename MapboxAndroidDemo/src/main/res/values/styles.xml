<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mapboxGrayDark10</item>
        <item name="colorPrimaryDark">@color/mapboxGrayExtraDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="menu_labels_style">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">@color/mapboxWhite</item>
    </style>

    <style name="CustomLocationComponent" parent="mapbox_LocationComponent">
        <item name="mapbox_foregroundDrawable">@drawable/android_custom_location_icon</item>

        <item name="mapbox_bearingDrawable">@drawable/ic_keyboard_arrow_up</item>
        <item name="mapbox_gpsDrawable">@drawable/ic_signal_wifi_4_bar</item>

        <item name="mapbox_accuracyAlpha">0.15</item>
        <item name="mapbox_accuracyColor">#FF82C6</item>

        <item name="mapbox_elevation">0dp</item>
        <item name="mapbox_compassAnimationEnabled">false</item>
        <item name="mapbox_accuracyAnimationEnabled">false</item>
    </style>

</resources>
